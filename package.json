{
  "name": "cross-agent-handoff",
  "version": "0.1.0",
  "description": "Cross-AI-client task handoff CLI. Save and restore task context between Claude Code, Codex, Cursor, and other AI coding agents.",
  "license": "MIT",
  "author": "ZCDeng",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ZCDeng/agentctx.git"
  },
  "type": "module",
  "bin": {
    "agentctx": "dist/cli.js"
  },
  "files": [
    "dist/",
    "skills/"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "clean": "rm -rf dist/",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "gray-matter": "^4.0.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
